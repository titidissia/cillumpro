let formatMesh = true;
let otherFeature = true;

if (formatMesh && otherFeature) {
    console.log("Both formatMesh and otherFeature are true.");
    // Additional code related to handling both features being true
} else {
    console.log("One or both conditions are false.");
    // Optional else block for handling cases where the condition is not met
}
